<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vazbloke.joytype">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="JoyType"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".MainActivity" />

        <service
            android:name="com.vazbloke.joytype.JoyTypeService"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <activity
            android:name="com.vazbloke.joytype.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight"
            android:label="JoyType Settings" />
        
        <activity android:name="com.vazbloke.joytype.CustomDictionaryActivity" android:theme="@style/Theme.AppCompat.NoActionBar"/>
    </application>
</manifest>